git:
  commitPrefixes:
    destiny-data-synchronizer: # This is repository folder name
      pattern: "^(\\w{4})\\/(\\w+-\\w+).*"
      replace: '$1: $2: '
    spock: # This is repository folder name
      pattern: "^(\\w{4})\\/(\\w+-\\w+).*"
      replace: '$1: $2: '
